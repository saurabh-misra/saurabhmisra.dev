---
title: Fix a column while scrolling horizontally in jQuery DataTable
date: 2023-09-29
slug: "/jquery-datatable-fixed-column"
tags:
  - "jQuery"
  - "DataTable"
  - "Quick Tip"
description: The "fixedColumns" property when set to "true" will fix the column marked as a header with "<th>" tags to act as a sticky column while scrolling the jQuery DataTable horizontally.
---

**This tutorial will teach you how to implement a fixed or sticky column in a <a href="https://datatables.net/" target="_blank">jQuery DataTable</a> while scrolling horizontally.**

First we'll setup a <a href="//jsfiddle.net/saurabhmisra87/0ew2ynkx/9" target="_blank">basic DataTable in jsFiddle</a>( with some <a href="https://datatables.net/examples/basic_init/scroll_xy.html" target="_blank">dummy data sourced from the official DataTable website</a>).  

In this setup, we have constrained the width with the help of CSS to produce the need for horizontal scrolling.

```css
div.dataTables_wrapper {
  width: 500px;
  margin: 0 auto;
}
```

Also, we have constrained the height in the DataTable configuration options. In these options, we have specified that we want to scroll both ways, horizontally as well as vertically and that the vertical height should be `500px` with the help of `scrollX` and `scrollY` properties.

```js
$(document).ready(function() {
  $("table.my-table").DataTable({
    pageLength: 100,
    scrollX: true,
    scrollY: 500
  });
});
```

## How to fix the first column while scrolling?
There are three things you must do in order to achieve this.

**<ins>Step 1</ins>**: Include the <a href="https://datatables.net/extensions/fixedcolumns/" target="_blank">FixedColumns Extension's CSS and JS</a>. 

```html
<link href="https://cdn.datatables.net/fixedcolumns/4.3.0/css/fixedColumns.dataTables.min.css" rel="stylesheet">
<script src="https://cdn.datatables.net/fixedcolumns/4.3.0/js/dataTables.fixedColumns.min.js"></script>
```

>The CSS and JS versions of the FixedColumns extension used above are the latest ones available at the time of writing this tutorial. You should use the version that is compatible with the version of the core DataTable plugins that you are using in your project.

<br/>
**<ins>Step 2</ins>**: Refactor the `<td>` tags of the column you want to fix as table headers i.e `<th>` tags.
<br/>
**<ins>Step 3</ins>**: Add the `fixedColumns` property to the DataTable options during initialization and set it to `true`.

```js highlight=6
$(document).ready(function() {
  $("table.my-table").DataTable({
    pageLength: 100,
    scrollX: true,
    scrollY: 500,
    fixedColumns: true
  });
});
```

This will enable the first column to be fixed while scrolling horizontally.

Check out <a href="//jsfiddle.net/saurabhmisra87/kry6c1o3/23" target="_blank">this solution in action in this jsFiddle</a>.

Hope this helps!🙏